import { NumerosFinancieraPipe } from './numeros-financiera.pipe';

describe('NumerosFinancieraPipe', () => {
  it('create an instance', () => {
    const pipe = new NumerosFinancieraPipe();
    expect(pipe).toBeTruthy();
  });
});
